# 2026-08-21

## 清理与 Pages 部署形态移除（用户确认无需备份）
- 删除：所有 *.gz（js/、css/、index.html.gz）、js/*.map（14 个）、frontend-dist/ 整目录（~50MB）
- 删除 Pages 入口：_worker.js、scripts/build-frontend-dist.mjs（git 显示 D）
- package.json：移除 pages:dev / build:frontend-dist；start→worker:dev；ci-test wait-on 端口 8080→8788；worker:dev/deploy/build:worker 去掉 build:frontend-dist 前缀
- copy-assets.mjs：去掉 frontend-dist 优先逻辑，始终从根目录取资源；asset-ignore.mjs 删除 frontend-dist 条目；.assetsignore 删除 _worker.js 行
- patch-tg-upload-performance.mjs：移除 frontend-dist 同步分支（根 bundle 已带 patch 标记，幂等）
- 测试同步：music-admin-password-ui / upload-chunk-merge-409（含删除死代码 map 读取）/ upstream-274-ui-guards（去 .gz 断言）/ worker-route-generation（端口断言）
- 验证：npm test 191 全过；copy-assets 正常（prune 仅剩 static/readme 43MB）；build:worker dry-run 四绑定齐全
- 注意：js/274.9b7364f3.js.map 等未追踪 map 已永久删除（无备份），如需恢复可从上游 marry-yellow/CloudFlare-ImgBed 获取
- .worktrees/ 下 node_modules 的 map 未动；README 仍含上游 Pages 部署说明未改
